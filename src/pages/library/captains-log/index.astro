---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { listPublishedForLibrary } from "../../../lib/content.js";

const entries = listPublishedForLibrary("captains-log");
---

<BaseLayout title="Captain's Log" noindex container="wide">
  <p class="eyebrow"><a href="/library">← Library</a></p>
  <h1>Captain's Log</h1>
  <p class="kicker" style="max-width: 34rem;">The expedition narrative — day by day, wherever "day" turns out to mean.</p>

  {entries.length === 0 ? (
    <p class="empty-note">No entries published yet. Check back soon.</p>
  ) : (
    <ul class="entry-list">
      {entries.map((entry) => (
        <li>
          <a class="card entry-card" href={`/library/captains-log/${entry.slug}`}>
            <p class="entry-meta">
              {entry.day_number != null && <span>Day {entry.day_number}</span>}
              {entry.expedition_date && <span>{entry.expedition_date}</span>}
              {entry.location && <span>{entry.location}</span>}
            </p>
            <h2>{entry.title}</h2>
            {entry.excerpt && <p class="muted">{entry.excerpt}</p>}
          </a>
        </li>
      ))}
    </ul>
  )}

  <style>
    .empty-note {
      border: 1px dashed var(--border);
      border-radius: var(--radius);
      padding: 2rem;
      color: var(--muted);
      text-align: center;
      margin-top: 2rem;
    }
    .entry-list { list-style: none; padding: 0; margin: 2rem 0 0; display: grid; gap: 1rem; }
    .entry-card { padding: 1.5rem 1.75rem; }
    .entry-meta {
      display: flex;
      gap: 0.75rem;
      flex-wrap: wrap;
      font-size: 0.78rem;
      font-weight: 700;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      color: var(--alv-turquoise-deep);
      margin-bottom: 0.5rem;
    }
    .entry-card h2 { margin: 0 0 0.4rem; }
  </style>
</BaseLayout>
